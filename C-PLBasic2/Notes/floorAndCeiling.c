#include<stdio.h>
#include<math.h>

int Floor(double x){
    for(int i=0 ; i<=x ; i++){

    }
}
    void main()
    {
        float n;
        printf("Enter the number:");
        scanf("%f",&n);
        float c =floor(n);
        float t= ceil(n);
        
        printf("%f %f",c , t);
        
           
}