#include<stdio.h>

void main()
{
    int over, wickets,targetScore  ;
    printf("The match is interrupted by the sudden rainfall. So here's the program to calculate the target score.");
    printf("Enter number of overs remaining:");
    scanf("%d", & over);
    printf("Enter number of wickets remaining.");
    scanf("%d", & wickets);

   
}